---
description: 创建一个新的 Lexicon 剧本（脚手架 + 数据 + 自动注册 + 起服务验证）
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, TaskCreate, TaskUpdate, Skill
---

调用 `new-script` skill。**不要**根据训练时的记忆自己实现剧本——交给 skill 处理，因为 skill 包含本仓库的最新数据契约、helpers 列表、文件布局规范。
